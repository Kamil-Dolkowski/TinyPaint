const drawingState = {
    currentColor: "#000",
    drawSize: 1,
    alpha: 1,
    font: "10px sans-serif",
    lineCap: "round",
    compositeOperation: "source-over",
    imageSmoothing: false,
}

export default drawingState;