const drawingState = {
    currentColor: "#000",
    drawSize: 5,
    alpha: 1,
    font: "10px sans-serif",
    lineCap: "round",
    compositeOperation: "source-over",
}

export default drawingState;